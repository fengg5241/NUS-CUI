

#include <math.h>
//a)
double area1(double base,double height){
    return base * height / 2;
}

//b)
double const PI=3.14159265;
double area2 (double A,double B,double ABAngle){
    return A * B * sin(ABAngle * PI/180.0) / 2.0;
}
    
//c)    
double area3 (double a,double b,double c){
    double s = (a + b + c)/2;
    return sqrt(s*(s-a)*(s-b)*(s-c));
}

//d)
They cannot be directly substituted for each other.
Because the number of parameters and meaning of parameters are different.


